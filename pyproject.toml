[project]
name = "deb-scripts"
version = "2.0.1"
description = "DevOps Automation Scripts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
